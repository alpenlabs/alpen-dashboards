[toolchain]
  # Matches alpen's pin so transitive `ruint`/`alloy` builds.
  channel = "nightly-2026-01-01"
  components = [
    "cargo",
    "clippy",
    "rust-analyzer",
    "rust-docs",
    "rust-src",
    "rust-std",
    "rustc",
    "rustfmt",
    # "miri" # some of the secp256k1 FFIs fail with miri
  ]
